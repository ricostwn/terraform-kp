---
- name: Create monitoring directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0755'
  loop:
    - /opt/monitoring
    - /opt/monitoring/prometheus
    - /opt/monitoring/grafana
    - /opt/monitoring/nginx
    - /opt/monitoring/cicd

- name: Create Prometheus configuration
  template:
    src: prometheus.yml.j2
    dest: /opt/monitoring/prometheus/prometheus.yml
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0644'

- name: Create Nginx load balancer configuration
  template:
    src: default.conf.j2
    dest: /opt/monitoring/nginx/default.conf
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0644'

- name: Create Docker Compose for monitoring stack
  copy:
    src: docker-compose.yml
    dest: /opt/monitoring/docker-compose.yml
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0644'

- name: Copy cicd script and make it executable
  copy:
    src: script.sh
    dest: /opt/monitoring/cicd/script.sh
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0755'

- name: Copy terraform-installer.sh script
  copy:
    src: terraform-installer.sh
    dest: /opt/monitoring/terraform-installer.sh
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0755'

- name: Run Terraform installer script
  shell: /opt/monitoring/terraform-installer.sh
  args:
    chdir: /opt/monitoring
  become: true

- name: Ensure ansible installed
  apt:
    name: ansible
    state: present

- name: Clone terraform repository
  become_user: "{{ ansible_user }}"
  ansible.builtin.git:
    repo: 'https://{{username}}:{{password}}@gitlab.53buahapel.web.id/iac/terraform.git'
    dest: /opt/monitoring/terraform
    version: main
    update: yes
    force: yes

- name: Start monitoring stack
  shell: cd /opt/monitoring && docker compose down && docker compose up -d
  become_user: "{{ ansible_user }}"
